%h1.clear Point Leaderboard

- @response.each do |group|
  %h3=group['type']
  %table.table.table-striped
    %thead
      %tr
        %th.center User
        %th.center Points
        %th.center Calories
        %th.center Steps
    %tbody
      - group['items'].each do |item|
        %tr
          %td
            %a
              %span.avatar-frame
                %img{src: item['user']['avatar']}
              %span.first_name
                = item['user']['first_name']
              %span.last_name
                = item['user']['last_name']
          %td.center
            %span.points.right
              = item['points']
          %td.center
            %span.calories.right
              = item['calories']
          %td.center
            %span.steps.right
              = item['steps']
